<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # If the request is not already in the public/ folder, rewrite it there
    RewriteCond %{REQUEST_URI} !^/evo/public/ [NC]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
